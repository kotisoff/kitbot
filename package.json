{
  "name": "kitbot",
  "version": "0.0.1",
  "main": "./dist/main.js",
  "repository": "https://github.com/kotisoff/kitbot.git",
  "author": "kotisoff",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node --no-warnings ./",
    "start": "node --no-warnings ./"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "discord.js": "^14.14.1"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
